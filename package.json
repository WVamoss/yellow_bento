{
  "name": "yellow-bento-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && mkdir -p public && cp -r frontend/dist/* public/",
    "start": "node api/admin.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.103.0"
  }
}
